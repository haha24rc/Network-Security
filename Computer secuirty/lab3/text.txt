Design your UI (user interface) for the secure chat solution. Your UI must allow the TA to see what message is actually sent and received over the wire at each point in the communication processes. Both chat client and server must show the following messages: Design your UI (user interface) for the secure chat solution. Your UI must allow the TA to see what message is actually sent and received over the wire at each point in the communication processes. Both chat client and server must show the following messages: Design your UI (user interface) for the secure chat solution. Your UI must allow the TA to see what message is actually sent and received over the wire at each point in the communication processes. Both chat client and server must show the following messages: abc